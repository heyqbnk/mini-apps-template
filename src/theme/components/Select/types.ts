export interface SelectColors {
  background: string;
  border: string;
  borderFocused: string;
  foreground: string;
  placeholder: string;
  icon: string;
}

export interface SelectTheme {
  colors: SelectColors;
}
