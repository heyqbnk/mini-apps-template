interface PaletteColor {
  primary: string;
  secondary: string;
}

/**
 * Palette colors
 */
export interface Palette {
  bodyBackground: string;
  bodyText: string;
  text: PaletteColor;
}
