/**
 * Device insets
 */
export interface Insets {
  top: number;
  bottom: number;
  left: number;
  right: number;
}

/**
 * Available operating systems
 */
export enum OS {
  IOS,
  Android
}
